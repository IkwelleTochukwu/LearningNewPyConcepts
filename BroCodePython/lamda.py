# lambda function = are written in one line using the 'lamda' keyword.
#                   accepts any number of arguments, but only as one expression.
#                   (think of it as a shortcut) It is useful for a short period of time, or throw-away.

# lambda parameters:expression

double = lambda x: x * 2
print(double(5))
